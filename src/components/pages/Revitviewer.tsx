import React from "react";

const RevitViewer: React.FC = () => {
  return (
    <div className="h-full w-full flex items-start">
      <span className="text-lg font-bold">Revit viewer</span>
      {/* Add your Revit viewer integration here */}
    </div>
  );
};

export default RevitViewer;
